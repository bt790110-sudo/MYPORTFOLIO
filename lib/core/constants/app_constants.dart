

class AppConstants {
  static const String appName = 'My Portfolio';
  static const String appVersion = '1.0.0';

  // Animations
  static const Duration animationDuration = Duration(milliseconds: 300);
  static const Duration pageTransitionDuration =
      Duration(milliseconds: 500);

  // Layout
  static const double maxWidth = 1400;
  static const double sectionPadding = 80.0;
  static const double mobileSectionPadding = 24.0;
}